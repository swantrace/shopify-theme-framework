<script>
window[window.themeName].themeElements = [
  {
    tagName: "cart-form-a",
    observedAttributes:  ["cart_json", "cart_is_updating", "old_cart_json"],
    definition: function(html, hooks, store, apis) {
      return function({cart_json, cart_is_updating, old_cart_json}){
        var cartFormRelatedData = hooks.useCartForm(cart_json, cart_is_updating, old_cart_json);
        var cart = cartFormRelatedData[0];
        var old_cart = cartFormRelatedData[1];
        var onQuantityInputChanged = cartFormRelatedData[2]['onQuantityInputChanged'];
        var onRemoveLinkClicked = cartFormRelatedData[2]['onRemoveLinkClicked'];
        var onUpdateButtonClicked = cartFormRelatedData[2]['onUpdateButtonClicked'];
        return html`{% render 'cart-form-a' %}`
      }
    }
  },
  {
    tagName: "add-to-cart-form",
    observedAttributes: ['product_handle', 'cart_is_updating'],
    definition: function(html, hooks, store, apis){ 
      return function({product_handle, cart_is_updating}) {
        var atcFormRelatedData = hooks.useATCForm(product_handle);
        var product = atcFormRelatedData[0];
        var currentVariant = atcFormRelatedData[1];
        var options = atcFormRelatedData[2];
        var onATCButtonClicked = atcFormRelatedData[3].onATCButtonClicked
        var onOptionChanged = atcFormRelatedData[3].onOptionChanged
        return html`{% render "add-to-cart-form" %}`;
      }
    }  
  },
  {
    tagName: 'collection-products-list',
    observedAttributes: ['product_handles'],
    definition: function(html, hooks, store, apis){ 
      return function(){
        return html``;
      }
    }
  }
]
</script>