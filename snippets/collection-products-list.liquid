${product_handles.map(product_handle => {
  return view_type == 'grid' ? html`<collection-product-grid-item style="max-width: 25%" product_handle=${product_handle}></collection-product-item>` : html`<collection-product-list-item product_handle=${product_handle}></collection-product-list-item>`
})}
