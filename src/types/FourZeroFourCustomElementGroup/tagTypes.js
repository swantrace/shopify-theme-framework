const fourZeroFourPageContent = {
  observedAttributes: [],
  hook: (id) => (store) => (element) => {
    return [];
  },
};

export default {
  'fourzerofour-page-content': fourZeroFourPageContent,
};
