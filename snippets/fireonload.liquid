<script>
  window.addEventListener('load', function () {
    App.init({
      config: {% render 'config' %},
      shopify: {% render 'shopify' %},
      customElementGroupDefinitions: {% render 'custom-elements'%},
      transformFns: {% render 'transform-fns'%},
      settings: theme.settings,
      locales: { ...theme.locales, currentLanguage: {{ request.locale.iso_code | json }} },
    })
    .then(app => {
      {% render 'global-callback' %}
    });
  });
</script>
