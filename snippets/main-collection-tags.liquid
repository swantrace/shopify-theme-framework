${all_tags.length>0?html`
<div class="form-horizontal">
  <label for="BrowseBy">{{ 'collections.sorting.browse' | t }}</label>
  <select name="BrowseBy" id="BrowseBy" class="btn--tertiary" @change=${onCurrentTagsChanged}>
    <option value="">{{ 'collections.sorting.all_tags' | t }}</option>
    ${all_tags.map(({label, handle}) => {
      return html`<option value=${handle} ?selected=${current_tags[0] === handle}>${label}</option>`
    })}
  </select>
</div>
`:html``}