${products.map(product => {
  return viewType == 'grid' ? 
  `<main-collection-product-grid-item style="max-width: 25%" product='${product}'></main-collection-product-item>` 
  : `<main-collection-product-list-item product='${product}''></main-collection-product-list-item>`
})}
