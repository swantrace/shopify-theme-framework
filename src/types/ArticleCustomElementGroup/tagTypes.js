/* eslint-disable no-unused-vars */
const articleBreadcrumb = {
  observedAttributes: [],
  hook: (id, helpers) => (store) => (element) => {
    return [];
  },
};

const articleMeta = {
  observedAttributes: [],
  hook: (id, helpers) => (store) => (element) => {
    return [];
  },
};

const articleTitle = {
  observedAttributes: [],
  hook: (id, helpers) => (store) => (element) => {
    return [];
  },
};

const articleContent = {
  observedAttributes: [],
  hook: (id, helpers) => (store) => (element) => {
    return [];
  },
};

const articleSocialSharing = {
  observedAttributes: [],
  hook: (id, helpers) => (store) => (element) => {
    return [];
  },
};

const articleBlogRecentArticles = {
  observedAttributes: [],
  hook: (id, helpers) => (store) => (element) => {
    return [];
  },
};

const articleBlogCategories = {
  observedAttributes: [],
  hook: (id, helpers) => (store) => (element) => {
    return [];
  },
};

export default {
  'article-breadcrumb': articleBreadcrumb,
  'article-meta': articleMeta,
  'article-title': articleTitle,
  'article-content': articleContent,
  'article-social-sharing': articleSocialSharing,
  'article-blog-recent-articles': articleBlogRecentArticles,
  'article-blog-categories': articleBlogCategories,
};
