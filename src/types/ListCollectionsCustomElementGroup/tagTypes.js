/* eslint-disable no-unused-vars */
const collectionsList = {
  observedAttributes: [],
  hook: (id, helpers) => (store) => (element) => {
    return [];
  },
};

const collectionsListItem = {
  observedAttributes: [],
  hook: (id, helpers) => (store) => (element) => {
    return [];
  },
};

const collectionsListBreadcrumb = {
  observedAttributes: [],
  hook: (id, helpers) => (store) => (element) => {
    return [];
  },
};

const collectionsListPagination = {
  observedAttributes: [],
  hook: (id, helpers) => (store) => (element) => {
    return [];
  },
};

export default {
  'collections-list': collectionsList,
  'collections-list-item': collectionsListItem,
  'collections-list-breadcrumb': collectionsListBreadcrumb,
  'collections-list-pagination': collectionsListPagination,
};
