/* eslint-disable no-unused-vars */
const indexSlidesShow = {
  observedAttributes: [],
  hook: (id) => (store) => (element) => {
    return [];
  },
};

const indexImageWithText = {
  observedAttributes: [],
  hook: (id) => (store) => (element) => {
    return [];
  },
};

const indexGallery = {
  observedAttributes: [],
  hook: (id) => (store) => (element) => {
    return [];
  },
};

const indexFeaturedProduct = {
  observedAttributes: [],
  hook: (id) => (store) => (element) => {
    return [];
  },
};

const indexFeaturedCollection = {
  observedAttributes: [],
  hook: (id) => (store) => (element) => {
    return [];
  },
};

const indexNewsletter = {
  observedAttributes: [],
  hook: (id) => (store) => (element) => {
    return [];
  },
};

const indexMap = {
  observedAttributes: [],
  hook: (id) => (store) => (element) => {
    return [];
  },
};

const indexRichText = {
  observedAttributes: [],
  hook: (id) => (store) => (element) => {
    return [];
  },
};

const indexVideo = {
  observedAttributes: [],
  hook: (id) => (store) => (element) => {
    return [];
  },
};

const indexCustomHTML = {
  observedAttributes: [],
  hook: (id) => (store) => (element) => {
    return [];
  },
};

const indexBlogPosts = {
  observedAttributes: [],
  hook: (id) => (store) => (element) => {
    return [];
  },
};

export default {
  'index-slideshow': indexSlidesShow,
  'index-image-with-text': indexImageWithText,
  'index-gallery': indexGallery,
  'index-featured-product': indexFeaturedProduct,
  'index-featured-collection': indexFeaturedCollection,
  'index-newsletter': indexNewsletter,
  'index-map': indexMap,
  'index-rich-text': indexRichText,
  'index-video': indexVideo,
  'index-custom-html': indexCustomHTML,
  'index-blog-posts': indexBlogPosts,
};
