/* eslint-disable no-unused-vars */
const passwordLoginModal = {
  observedAttributes: [],
  hook: (id) => (store) => (element) => {
    return [];
  },
};

const passwordFooter = {
  observedAttributes: [],
  hook: (id) => (store) => (element) => {
    return [];
  },
};

const passwordHeader = {
  observedAttributes: [],
  hook: (id) => (store) => (element) => {
    return [];
  },
};

const passwordContent = {
  observedAttributes: [],
  hook: (id) => (store) => (element) => {
    return [];
  },
};

export default {
  'password-login-modal': passwordLoginModal,
  'password-footer': passwordFooter,
  'passowrd-header': passwordHeader,
  'password-content': passwordContent,
};
