/* eslint-disable no-unused-vars */
const cartForm = {
  observedAttributes: [],
  hook: (id, helpers) => (store) => (element) => {
    return [];
  },
};

export default {
  'cart-form': cartForm,
};
